(()=>{const spend=document.getElementById('fuelSpend');const rate=document.getElementById('rewardRate');const spendOut=document.getElementById('spendValue');const rateOut=document.getElementById('rateValue');const annual=document.getElementById('annualReward');if(!spend||!rate)return;const money=new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0});function update(){const monthly=Number(spend.value);const pct=Number(rate.value);spendOut.textContent=money.format(monthly);rateOut.textContent=`${pct}%`;annual.textContent=money.format(monthly*12*pct/100)}spend.addEventListener('input',update);rate.addEventListener('input',update);update()})();
